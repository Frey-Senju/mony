# Story 1.2: Backend Authentication & User Management

**Status**: Draft  
**Story ID**: 1.2  
**Epic**: EPIC-001  
**Assigned**: @dev  
**Created**: 2026-04-15  
**Priority**: P0 (Blocks frontend & API)

## Description

Implement secure JWT-based authentication system for Mony with:

1. **User Registration & Login** - Secure password hashing, JWT tokens
2. **Subscription Plans** - Basic/Pro/Premium tiers (like Pierre Finance)
3. **Open Finance Integration** - Preparation for bank connections (Story 2.0)
4. **Two-Factor Authentication** - Optional 2FA security
5. **Session Management** - Refresh tokens, token expiration
6. **User Profile Management** - Settings, preferences, data
7. **Security Hardening** - Rate limiting, CORS, password validation
8. **Audit Logging** - Track all authentication events

## Acceptance Criteria

### Authentication
- [ ] User registration endpoint with email/password
- [ ] Password hashing using bcrypt (cost factor 12+)
- [ ] Login endpoint returning JWT token pair (access + refresh)
- [ ] Token refresh endpoint (7-day expiration cycle)
- [ ] Token validation middleware on protected routes
- [ ] Logout endpoint (token blacklist optional)
- [ ] Password reset flow (email verification)
- [ ] Email verification before account activation

### Subscription Plans
- [ ] Plan types: BASIC (free), PRO, PREMIUM
- [ ] Plan limits enforced:
  - BASIC: 1 bank, 1 spending alert, basic reports
  - PRO: 5 banks, 5 alerts, advanced reports
  - PREMIUM: 20 banks, unlimited alerts, all features
- [ ] Plan status tracking (active/expired/canceled)
- [ ] Subscription history recording
- [ ] Plan upgrade/downgrade endpoints

### Open Finance Preparation
- [ ] Open Finance connection model created
- [ ] Institution list endpoint (Brazilian banks)
- [ ] Consent initiation flow prepared
- [ ] Sync status tracking
- [ ] Error handling for failed connections
- [ ] (Actual sync deferred to Story 2.0)

### Security
- [ ] Rate limiting (login attempts, registration)
- [ ] CORS configuration
- [ ] CSRF protection
- [ ] Password requirements (min 8 chars, complexity)
- [ ] Account lockout after failed attempts
- [ ] Login history tracking
- [ ] IP address logging
- [ ] User agent logging

### Audit & Logging
- [ ] All logins logged (success/failure)
- [ ] Plan changes logged
- [ ] Profile updates logged
- [ ] Password changes logged
- [ ] Security events logged

### Testing
- [ ] Unit tests for password hashing
- [ ] Integration tests for auth endpoints
- [ ] Token validation tests
- [ ] Rate limiting tests
- [ ] Plan enforcement tests
- [ ] >85% code coverage

## Technical Implementation

### Dependencies

```
pip install:
- python-jose==3.3.0  # JWT encoding/decoding
- passlib==1.7.4     # Password hashing
- bcrypt==4.0.1      # Bcrypt algorithm
- python-multipart   # Form data parsing
- slowapi==0.1.8     # Rate limiting
```

### File Structure

```
apps/api/
├── core/
│   ├── __init__.py
│   ├── security.py          # Password hashing, JWT, token utils
│   ├── config.py            # Settings, JWT secrets
│   ├── exceptions.py        # Custom exceptions
│   └── constants.py         # Constants (token expiration, etc)
├── api/
│   ├── auth/
│   │   ├── __init__.py
│   │   ├── router.py        # Auth endpoints
│   │   ├── schemas.py       # Pydantic models
│   │   ├── service.py       # Business logic
│   │   ├── dependencies.py  # FastAPI dependencies
│   │   └── tests/
│   │       ├── test_auth.py
│   │       └── test_security.py
│   ├── users/
│   │   ├── router.py        # User profile endpoints
│   │   ├── schemas.py       # User schemas
│   │   └── service.py       # User business logic
│   └── subscriptions/
│       ├── router.py        # Plan management
│       ├── schemas.py       # Subscription schemas
│       └── service.py       # Plan logic
├── main.py                  # FastAPI app with middleware
└── requirements.txt         # Update with new deps
```

### 1. Security Module (core/security.py)

```python
from passlib.context import CryptContext
from datetime import timedelta
from jose import JWTError, jwt
from typing import Optional

# Password hashing
pwd_context = CryptContext(
    schemes=["bcrypt"],
    deprecated="auto",
    bcrypt__rounds=12  # Security strength
)

def hash_password(password: str) -> str:
    """Hash password with bcrypt."""
    return pwd_context.hash(password)

def verify_password(plain: str, hashed: str) -> bool:
    """Verify password against hash."""
    return pwd_context.verify(plain, hashed)

# JWT tokens
def create_access_token(
    user_id: str,
    expires_delta: Optional[timedelta] = None
) -> str:
    """Create JWT access token."""
    if expires_delta is None:
        expires_delta = timedelta(minutes=15)
    return jwt.encode(
        {"sub": user_id, "exp": datetime.utcnow() + expires_delta},
        settings.SECRET_KEY,
        algorithm="HS256"
    )

def create_refresh_token(user_id: str) -> str:
    """Create JWT refresh token (7 days)."""
    return jwt.encode(
        {"sub": user_id, "type": "refresh", "exp": datetime.utcnow() + timedelta(days=7)},
        settings.REFRESH_SECRET_KEY,
        algorithm="HS256"
    )

def verify_token(token: str) -> dict:
    """Verify and decode JWT token."""
    try:
        return jwt.decode(
            token,
            settings.SECRET_KEY,
            algorithms=["HS256"]
        )
    except JWTError:
        raise InvalidTokenException()
```

### 2. Auth Endpoints (api/auth/router.py)

```python
@router.post("/register", response_model=UserResponse)
async def register(
    request: RegisterRequest,
    db: Session = Depends(get_db)
):
    """User registration."""
    # Validate email not exists
    # Validate password strength
    # Hash password
    # Create user
    # Send verification email
    # Return user + tokens

@router.post("/login", response_model=LoginResponse)
async def login(
    request: LoginRequest,
    db: Session = Depends(get_db)
):
    """User login."""
    # Validate credentials
    # Check email verified
    # Check account not locked
    # Create access + refresh tokens
    # Log login event
    # Return tokens + user

@router.post("/refresh")
async def refresh_token(
    request: RefreshRequest,
    db: Session = Depends(get_db)
):
    """Refresh access token using refresh token."""
    # Verify refresh token
    # Create new access token
    # Return new token

@router.post("/logout")
async def logout(
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """Logout (token blacklist)."""
    # Add token to blacklist
    # Clear session

@router.post("/password-reset")
async def reset_password(
    request: PasswordResetRequest,
    db: Session = Depends(get_db)
):
    """Initiate password reset."""
    # Generate reset token
    # Send reset email
    # Return confirmation

@router.post("/password-reset/{token}")
async def confirm_password_reset(
    token: str,
    request: PasswordResetConfirm,
    db: Session = Depends(get_db)
):
    """Confirm password reset with token."""
    # Verify token valid
    # Hash new password
    # Update user
    # Log event
```

### 3. User Profile Endpoints (api/users/router.py)

```python
@router.get("/me", response_model=UserResponse)
async def get_current_user_profile(
    current_user: User = Depends(get_current_user)
):
    """Get current user profile."""
    return current_user

@router.put("/me", response_model=UserResponse)
async def update_profile(
    request: UpdateProfileRequest,
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """Update user profile (name, phone, preferences)."""
    # Update non-sensitive fields
    # Log change in audit_log
    # Return updated user

@router.post("/me/email-verify")
async def send_email_verification(
    current_user: User = Depends(get_current_user)
):
    """Send email verification code."""
    # Generate verification code
    # Send email
    # Return confirmation

@router.post("/me/email-verify/{code}")
async def verify_email(
    code: str,
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """Verify email with code."""
    # Check code valid
    # Mark email as verified
    # Log event
```

### 4. Subscription Endpoints (api/subscriptions/router.py)

```python
@router.get("/plans")
async def list_plans():
    """Get available subscription plans."""
    return {
        "plans": [
            {
                "id": "basic",
                "name": "Básico",
                "price": 0.00,
                "billing_period": "lifetime",
                "limits": {
                    "banks": 1,
                    "alerts": 1,
                    "reports": "basic"
                }
            },
            {
                "id": "pro",
                "name": "Profissional",
                "price": 29.90,
                "billing_period": "monthly",
                "limits": {
                    "banks": 5,
                    "alerts": 5,
                    "reports": "advanced"
                }
            },
            {
                "id": "premium",
                "name": "Premium",
                "price": 99.90,
                "billing_period": "monthly",
                "limits": {
                    "banks": 20,
                    "alerts": -1,  # unlimited
                    "reports": "all"
                }
            }
        ]
    }

@router.get("/me/subscription", response_model=SubscriptionResponse)
async def get_current_subscription(
    current_user: User = Depends(get_current_user)
):
    """Get current user subscription details."""
    return {
        "plan": current_user.plan,
        "started_at": current_user.plan_started_at,
        "expires_at": current_user.plan_expires_at,
        "is_active": current_user.is_plan_active,
        "auto_renew": True
    }

@router.post("/upgrade")
async def upgrade_plan(
    request: UpgradeRequest,
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """Upgrade subscription plan."""
    # Validate new plan
    # Process payment (if needed)
    # Update user.plan
    # Record in subscription_history
    # Log event
    # Return confirmation

@router.get("/me/subscription-history")
async def get_subscription_history(
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """Get user's subscription change history."""
    return db.query(SubscriptionHistory)\
        .filter_by(user_id=current_user.id)\
        .order_by(SubscriptionHistory.created_at.desc())\
        .all()
```

### 5. Rate Limiting

```python
from slowapi import Limiter
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)

# Apply to auth endpoints
@router.post("/register")
@limiter.limit("5/minute")  # 5 registrations per minute per IP
async def register(request: Request, ...):
    pass

@router.post("/login")
@limiter.limit("10/minute")  # 10 login attempts per minute
async def login(request: Request, ...):
    pass
```

### 6. Middleware & Dependencies

```python
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials

security = HTTPBearer()

async def get_current_user(
    credentials: HTTPAuthorizationCredentials = Depends(security),
    db: Session = Depends(get_db)
) -> User:
    """Dependency to get current authenticated user."""
    token = credentials.credentials
    payload = verify_token(token)
    user_id = payload.get("sub")
    
    user = db.query(User).filter(User.id == user_id).first()
    if not user or user.deleted_at:
        raise NotAuthenticatedException()
    
    return user
```

## Database Changes

### Schema Updates

**Users table additions**:
```sql
ALTER TABLE users ADD COLUMN plan VARCHAR(20) DEFAULT 'basic';
ALTER TABLE users ADD COLUMN plan_started_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP;
ALTER TABLE users ADD COLUMN plan_expires_at TIMESTAMP;
ALTER TABLE users ADD COLUMN openfinance_status VARCHAR(50) DEFAULT 'not_connected';
ALTER TABLE users ADD COLUMN openfinance_token VARCHAR(500);
ALTER TABLE users ADD COLUMN openfinance_institutions FLOAT DEFAULT 0;
ALTER TABLE users ADD COLUMN openfinance_last_sync TIMESTAMP;
ALTER TABLE users ADD COLUMN last_login_at TIMESTAMP;
ALTER TABLE users ADD COLUMN last_login_ip VARCHAR(45);
ALTER TABLE users ADD COLUMN two_factor_enabled BOOLEAN DEFAULT FALSE;
ALTER TABLE users ADD COLUMN two_factor_secret VARCHAR(32);
```

**New tables**:
```sql
CREATE TABLE openfinance_connections (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  institution_code VARCHAR(50),
  institution_name VARCHAR(255),
  status VARCHAR(50),
  ...
);

CREATE TABLE subscription_history (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  plan_from VARCHAR(20),
  plan_to VARCHAR(20),
  amount_paid DECIMAL(15,2),
  ...
);
```

## File List

### To Create
- [ ] `apps/api/core/security.py` - Password/JWT utilities
- [ ] `apps/api/core/config.py` - Settings & environment
- [ ] `apps/api/core/exceptions.py` - Custom exceptions
- [ ] `apps/api/core/constants.py` - Constants
- [ ] `apps/api/core/__init__.py` - Package init
- [ ] `apps/api/api/auth/router.py` - Auth endpoints
- [ ] `apps/api/api/auth/schemas.py` - Pydantic models
- [ ] `apps/api/api/auth/service.py` - Business logic
- [ ] `apps/api/api/auth/dependencies.py` - FastAPI deps
- [ ] `apps/api/api/auth/__init__.py` - Package init
- [ ] `apps/api/api/auth/tests/__init__.py`
- [ ] `apps/api/api/auth/tests/test_auth.py` - Auth tests
- [ ] `apps/api/api/users/router.py` - User endpoints
- [ ] `apps/api/api/users/schemas.py` - User schemas
- [ ] `apps/api/api/users/service.py` - User logic
- [ ] `apps/api/api/subscriptions/router.py` - Plan endpoints
- [ ] `apps/api/api/subscriptions/schemas.py` - Schemas
- [ ] `apps/api/api/__init__.py` - API router
- [ ] `apps/api/main.py` - FastAPI app (update)
- [ ] `apps/api/.env.example` - Update with new vars

### To Update
- [ ] `apps/api/requirements.txt` - Add new dependencies
- [ ] `apps/api/database/models.py` - Already updated ✅
- [ ] `apps/api/database/__init__.py` - Already updated ✅
- [ ] `packages/shared/src/types.ts` - Add TypeScript types

### Documentation
- [ ] `docs/API.md` - Add auth endpoints (update)
- [ ] `docs/AUTHENTICATION.md` - Auth guide (new)
- [ ] `docs/PIERRE_FEATURES.md` - Pierre Finance features mapping

## Dependencies

- Story 1.1: Database Schema (✅ COMPLETED)
- PostgreSQL running
- Python 3.11+
- FastAPI 0.104+

## Implementation Plan

**Phase 1: Security Foundation** (3 hours)
- [ ] Create core/security.py with password & JWT utils
- [ ] Create core/config.py with settings
- [ ] Create core/exceptions.py
- [ ] Create core/constants.py
- [ ] Add to requirements.txt

**Phase 2: Auth Endpoints** (4 hours)
- [ ] Create auth/schemas.py (Pydantic models)
- [ ] Create auth/service.py (business logic)
- [ ] Create auth/dependencies.py (FastAPI deps)
- [ ] Create auth/router.py (endpoints)
- [ ] Add rate limiting
- [ ] Add CORS configuration

**Phase 3: User Management** (3 hours)
- [ ] Create users/router.py (profile endpoints)
- [ ] Create users/service.py (user logic)
- [ ] Implement profile update
- [ ] Implement email verification

**Phase 4: Subscriptions** (2 hours)
- [ ] Create subscriptions/router.py (plan endpoints)
- [ ] Implement plan enforcement
- [ ] Implement plan upgrade
- [ ] Subscription history

**Phase 5: Testing & Refinement** (3 hours)
- [ ] Write unit tests (security)
- [ ] Write integration tests (endpoints)
- [ ] Test rate limiting
- [ ] Load testing
- [ ] Security audit

## Definition of Done

- [ ] All 6+ auth endpoints implemented
- [ ] Passwords hashed with bcrypt (cost 12)
- [ ] JWT tokens working (access + refresh)
- [ ] Token validation on protected routes
- [ ] Rate limiting active (login, register)
- [ ] CORS properly configured
- [ ] Plan system enforced
- [ ] Open Finance structure ready
- [ ] Audit logging working
- [ ] 90%+ code coverage
- [ ] All tests passing
- [ ] Security audit passed
- [ ] Documentation complete
- [ ] Ready for Story 1.3 (Frontend Auth)

## Security Considerations

### Password Requirements
- Minimum 8 characters
- At least 1 uppercase letter
- At least 1 lowercase letter
- At least 1 number
- At least 1 special character

### JWT Configuration
- Access token: 15 minutes expiration
- Refresh token: 7 days expiration
- Secret key: 32+ random characters
- Algorithm: HS256

### Rate Limiting
- Registration: 5 per minute per IP
- Login: 10 per minute per IP
- Password reset: 3 per hour per IP
- Email verification: 5 per day per user

### Account Security
- Lockout after 5 failed login attempts (15 minutes)
- Email verification required before full access
- Password reset via email only
- 2FA optional (implementation ready)
- Login history tracked

## Pierre Finance Features (Implemented)

| Feature | Mony Implementation | Status |
|---------|-------------------|--------|
| **Bank Integration** | Open Finance connections | ✅ Schema ready |
| **Multiple Plans** | Basic/Pro/Premium | ✅ This story |
| **Spending Analysis** | Via categories + limits | ✅ Story 1.1 |
| **Budgeting** | Spending limits feature | ✅ Story 1.1 |
| **Insights** | AI insights (future) | ⏳ Story 2.0+ |
| **Security** | JWT + bcrypt + audit | ✅ This story |
| **Compliance** | Audit log + soft deletes | ✅ This story |

---

**Estimated Time**: 15 hours  
**Complexity**: High  
**Blocker**: No (parallel: Story 1.3 can start after endpoints)

**Next Story**: 1.3 (Frontend Authentication UI)

---

*Story Status: Ready for @dev implementation*
